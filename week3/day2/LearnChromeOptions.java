package week3.day2;

import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.chrome.ChromeOptions;

public class LearnChromeOptions {

	public static void main(String[] args) {
		/*
		 * System.setProperty("webdriver.chrome.driver", "./drivers/chromedriver.exe");
		 * 
		 * ChromeOptions options = new ChromeOptions();
		 * options.addArguments("--disable-notifications");
		 * //options.addArguments("headless");
		 * options.addArguments("--disable-infobars"); ChromeDriver driver = new
		 * ChromeDriver(options); driver.get("https://www.redbus.in/");
		 * driver.manage().window().maximize();
		 * 
		 * driver.findElementByLinkText("BUS TICKETS").click(); String title =
		 * driver.getTitle(); System.out.println(title); driver.close();
		 */

	}

}
